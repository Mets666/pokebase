{% extends 'base.html.twig'%}

{% block title %} Add new pokemon{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-8">.col-md-8</div>
        <div class="col-md-4">.col-md-4</div>
    </div>
    <h1>Add new pokemon:</h1>

    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}